server:
  port: 9091
  
eureka:
  instance:
    hostname: localhost
  client:
    registerWhithEureka: true;
    fetchRegistry: false
    serviceUrl:
      defaultZone: hottp:// ${eureka.instance.hostname}:${server.port}/eureka/
    server:
      wait-time-in-ms-when-sync-empty: 3000
